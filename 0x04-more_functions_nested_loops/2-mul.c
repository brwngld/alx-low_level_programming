#inlcude "main.h"
#include <stdio.h>

/**
 * int mul - A function that multiplies two integers.
 * written by brwngld
 * @a: integer
 * @b: integer
 * Return: multiplication of a and b
*/

int mul(int a, int b);
{
	_putchar('a' + 0);
	_putchar('b' + 0);

	return (a * b);
}
