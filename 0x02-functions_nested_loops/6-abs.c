#include "main.h"

/**
 * _abs - Computes the absolute value of an integer
 * @n: the int absolute to be determined
 *
 * Return: the abs of the int.
*/

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (n);
}
